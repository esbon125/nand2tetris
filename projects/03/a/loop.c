#include "stdio.h"

int main(){
for (int i=0; i<16; i++) {
printf("DMux8Way(in=in[%d], sel=load, a=a[%d], b=b[%d], c=c[%d], d=d[%d], e=e[%d], f=f[%d], g=g[%d], h=h[%d]);\n",i,i,i,i,i,i,i,i,i);
}

    return 0;
}